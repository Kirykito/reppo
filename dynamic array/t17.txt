int main() {
    int_vector v;
    int num;

    while (std::cin >> num && num != 0) {
        v.push_back(num);
    }

    for (int i = v.size() - 1; i >= 0; i--) {
        std::cout << v.at(i) << " ";
    }

    return 0;
}