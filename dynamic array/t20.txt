void remove_all(int value) {
    int write_index = 0;
    
    for (int i = 0; i < size_; i++) {
        if (data_[i] != value) {
            data_[write_index] = data_[i];
            write_index++;
        }
    }
    
    size_ = write_index;
}