void reverse() {
    for (int i = 0; i < size_ / 2; i++) {
        (size - 1 - i)
        int temp = data_[i];
        data_[i] = data_[size_ - 1 - i];
        data_[size_ - 1 - i] = temp;
    }
}